"""
🤖 OmniCRM God Mode - نظام الذكاء الاصطناعي المرن
=====================================================

## 🎯 نظرة عامة

OmniCRM يدعم **6 محركات ذكاء اصطناعي** (3 مجانية + 3 مدفوعة) مع إمكانية التبديل التلقائي!

## 📊 المحركات المتاحة

### ✅ **محركات مجانية (Free)**

1. **Ollama (Local AI)**
   - التكلفة: 0$ (محلي على جهازك)
   - السرعة: سريع جداً
   - الخصوصية: 100% (البيانات لا تخرج من السيرفر)
   - الموديلات المدعومة:
     * llama3.2 (3B/7B)
     * mistral
     * gemma2
     * qwen2.5
   
   **التثبيت:**
   ```bash
   # تثبيت Ollama
   curl -fsSL https://ollama.com/install.sh | sh
   
   # تحميل موديل
   ollama pull llama3.2:7b
   
   # تشغيل الخدمة
   ollama serve
   ```
   
   **إعداد في OmniCRM:**
   ```bash
   # في ملف .env
   AI_PROVIDER=ollama
   OLLAMA_BASE_URL=http://localhost:11434
   OLLAMA_MODEL=llama3.2:7b
   ```

2. **Groq (Free Tier)**
   - التكلفة: 0$ (حتى حد معين)
   - السرعة: **أسرع محرك على الإطلاق** (300+ tokens/sec)
   - الموديلات:
     * llama-3.3-70b-versatile
     * mixtral-8x7b
     * gemma2-9b
   
   **الحصول على المفتاح:**
   ```
   1. اذهب إلى: https://console.groq.com
   2. سجل دخول مجاناً
   3. انسخ API Key من: API Keys → Create API Key
   ```
   
   **إعداد:**
   ```bash
   AI_PROVIDER=groq
   GROQ_API_KEY=gsk_your_free_api_key_here
   ```

3. **Google Gemini Free Tier**
   - التكلفة: 0$ (15 requests/minute مجاناً)
   - الجودة: ممتازة
   - الموديل: gemini-2.0-flash-exp
   
   **الحصول على المفتاح:**
   ```
   1. https://makersuite.google.com/app/apikey
   2. Create API key in new project
   ```
   
   **إعداد:**
   ```bash
   AI_PROVIDER=google
   GOOGLE_API_KEY=AIza...your_key
   ```

---

### 💰 **محركات مدفوعة (Premium)**

4. **OpenAI GPT-4o**
   - التكلفة: ~$0.10 لكل 100 رسالة
   - الجودة: الأفضل للعربية
   - الموديل: gpt-4o
   
   **الحصول على المفتاح:**
   ```
   1. https://platform.openai.com/api-keys
   2. Create new secret key
   3. أضف رصيد (minimum $5)
   ```
   
   **إعداد:**
   ```bash
   AI_PROVIDER=openai
   OPENAI_API_KEY=sk-proj-your_key_here
   ```

5. **Anthropic Claude**
   - التكلفة: ~$0.08 لكل 100 رسالة
   - الجودة: ممتاز للتحليل
   - الموديل: claude-3-5-sonnet
   
   **الحصول على المفتاح:**
   ```
   1. https://console.anthropic.com/
   2. Get API Keys
   ```
   
   **إعداد:**
   ```bash
   AI_PROVIDER=anthropic
   ANTHROPIC_API_KEY=sk-ant-your_key
   ```

6. **Google Gemini Pro**
   - التكلفة: ~$0.05 لكل 100 رسالة
   - الجودة: جيد جداً
   - الموديل: gemini-2.0-flash-exp
   
   **إعداد:**
   ```bash
   AI_PROVIDER=google
   GOOGLE_API_KEY=AIza...your_paid_key
   ```

---

## 🔄 التبديل التلقائي (Fallback System)

OmniCRM يحاول المحركات بالترتيب حتى ينجح:

```python
# في app/core/config.py
AI_PROVIDER=auto  # التبديل التلقائي

# الترتيب:
1. OpenAI GPT-4o (إذا كان المفتاح موجود)
2. Claude (إذا فشل OpenAI)
3. Gemini (إذا فشل Claude)
4. Groq (إذا فشل Gemini)
5. Ollama (الخيار المحلي الأخير)
```

---

## 💡 استراتيجيات توفير التكلفة

### **الخيار 1: مجاني 100%**
```bash
# استخدم Ollama محلياً
AI_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:7b

# التكلفة الشهرية: 0$
# الأداء: جيد جداً
# الخصوصية: 100%
```

### **الخيار 2: شبه مجاني**
```bash
# استخدم Groq (أسرع + مجاني)
AI_PROVIDER=groq
GROQ_API_KEY=gsk_your_free_key

# التكلفة: 0$ (ضمن الحد المجاني)
# السرعة: ⚡ الأسرع
```

### **الخيار 3: هجين (موصى به)**
```bash
# استخدم Groq للعمليات السريعة
# + Ollama للخصوصية
# + OpenAI للحالات الحرجة فقط

AI_PROVIDER=auto
GROQ_API_KEY=gsk_...
OLLAMA_BASE_URL=http://localhost:11434
OPENAI_API_KEY=sk-proj-...  # للطوارئ فقط

# التكلفة المتوقعة: $5-10/شهر
```

---

## 🛠️ تغيير محرك الذكاء الاصطناعي

### **الطريقة 1: من لوحة التحكم**
```
1. اذهب إلى: الإعدادات > الذكاء الاصطناعي
2. اختر المحرك من القائمة المنسدلة
3. أدخل API Key (إذا لزم)
4. اضغط "حفظ واختبار"
```

### **الطريقة 2: من ملف .env**
```bash
# عدّل الملف
nano .env

# غيّر المحرك
AI_PROVIDER=groq

# احفظ وأعد التشغيل
docker-compose restart app
```

### **الطريقة 3: من API**
```bash
curl -X POST http://localhost:8000/api/admin/settings \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "ai_provider": "groq",
    "groq_api_key": "gsk_..."
  }'
```

---

## 📊 مقارنة الأداء

| المحرك | السرعة | الجودة | التكلفة/100 رسالة | الأفضل لـ |
|--------|--------|--------|-------------------|----------|
| **Ollama** | ⚡⚡⚡⚡ | ⭐⭐⭐ | $0 | الخصوصية |
| **Groq** | ⚡⚡⚡⚡⚡ | ⭐⭐⭐⭐ | $0 | السرعة |
| **Gemini Free** | ⚡⚡⚡ | ⭐⭐⭐⭐ | $0 | التوازن |
| **OpenAI GPT-4o** | ⚡⚡ | ⭐⭐⭐⭐⭐ | $0.10 | الجودة |
| **Claude** | ⚡⚡⚡ | ⭐⭐⭐⭐⭐ | $0.08 | التحليل |
| **Gemini Pro** | ⚡⚡⚡ | ⭐⭐⭐⭐ | $0.05 | السعر |

---

## 🔐 أمان المفاتيح

**⚠️ مهم جداً:**

```bash
# ❌ لا تكتب المفاتيح في الكود
OPENAI_API_KEY=sk-proj-123...  # ❌ خطأ

# ✅ استخدم ملف .env
echo "OPENAI_API_KEY=sk-proj-123..." >> .env

# ✅ أو متغيرات النظام
export OPENAI_API_KEY=sk-proj-123...

# ✅ أو Docker secrets
docker secret create openai_key ./openai_key.txt
```

---

## 🧪 اختبار المحركات

```bash
# اختبر جميع المحركات
python scripts/test_ai_providers.py

# اختبر محرك معين
python scripts/test_ai_providers.py --provider groq

# نتيجة متوقعة:
✅ Ollama: Working (250ms)
✅ Groq: Working (80ms) ⚡ FASTEST
✅ Gemini: Working (180ms)
❌ OpenAI: API key not configured
❌ Claude: API key not configured
```

---

## 💰 حساب التكلفة المتوقعة

### **سيناريو 1: متجر صغير (100 عميل/يوم)**
```
عدد الرسائل اليومية: ~200
المحرك: Groq (مجاني)
التكلفة الشهرية: $0 ✅
```

### **سيناريو 2: متجر متوسط (500 عميل/يوم)**
```
عدد الرسائل اليومية: ~1000
المحرك: Groq (مجاني) + Ollama (احتياطي)
التكلفة الشهرية: $0 - $5 ✅
```

### **سيناريو 3: متجر كبير (2000 عميل/يوم)**
```
عدد الرسائل اليومية: ~5000
المحرك: OpenAI GPT-4o (للجودة)
التكلفة الشهرية: ~$150
البديل: Groq + Ollama → $0 - $10 ✅
```

---

## 🚀 التوصية النهائية

**للبداية (0$ تكلفة):**
```bash
AI_PROVIDER=groq
GROQ_API_KEY=gsk_free_key_here
```

**للتوسع (توازن السعر/الجودة):**
```bash
AI_PROVIDER=auto
GROQ_API_KEY=gsk_...        # للسرعة
OLLAMA_BASE_URL=http://...  # للخصوصية
OPENAI_API_KEY=sk-proj-...  # للجودة العالية
```

**للجودة القصوى (لا تهم التكلفة):**
```bash
AI_PROVIDER=openai
OPENAI_API_KEY=sk-proj-...
```

---

## 📞 دعم إضافي

- **التوثيق الكامل:** https://github.com/admragy/NeuroCRM-GodMode/wiki/AI-Providers
- **مشاكل شائعة:** https://github.com/admragy/NeuroCRM-GodMode/issues
- **مجتمع Discord:** (قريباً)

---

**✅ النظام مرن 100% - اختر ما يناسب ميزانيتك!**
